import React from "react";
import { Navigation } from "../Navigation/Navigation";
import { Search } from "../Search/Search";

export const Header = () => {
  return (
    <div>
      <Navigation />
      <Search />
    </div>
  );
};
